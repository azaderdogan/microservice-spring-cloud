server:
  port: 8888

spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/microservice
        default-profile: native

      discovery:
        enabled: false
  profiles:
    active: native
  application:
    name: config-server

