spring:
  profiles:
    active: native
  application:
    name: account-service
  cloud:
    config:
      uri: ${SPRING_CONFIG_URI:http://localhost:8888}
      override-none: true
      override-system-properties: false
